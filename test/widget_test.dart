// Placeholder smoke test. Real unit tests (split engine, balances) live in
// test/split_engine_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
